# Genera el hash con:
# node generate-hash.js "tu-password-segura"
APP_PASSWORD_HASH=

# Genera el secreto con:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=

PORT=4000
ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
DB_FILE=./data.sqlite
